/* Hello World */

int main() {
    int x, y;
    x = input();
    y = 10;
    
    if (x == y) {
        2output(x);
    } else {
        output(y);
    }
    
    while (x < y) {
        x = x + 1;
    }
    
    return 0;
}$

int main